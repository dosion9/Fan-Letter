const initialState = {
  type: "default",
  active: false,
  content: null,
  onSummit: null
};

const modalStste = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default modalStste;
